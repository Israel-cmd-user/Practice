What I need to change is as follows, in the culvertInspection form once the user enters the number of cells in the structure information that number gets auto populated in structure information as well. By default the number of rows under specific positions is 2, however if the user enters 5 for instance that number then becomes the number of rows within that table. I addition there are choices files located under a folder called choices under components. There is a global choices file that contains items that are used in both forms. There are fields already exiting as text input can you then add the choices from the 3 choices files to accommodate those fields, bridge inspection already has some filled you can just use that as reference to fill in the rest of the other forms. Another  thing is there is position miimum clearance and position of expansion joints under the bridgeInventor. Those check boxes specifically S1 and S2, the number of check boxes are determined by the number of spans entered. Same applies to Position of minimum clearance  under the culvertInventory, the number of check boxes S1 and S2 is determined by the number of cells entered by the user.

Show me a plan before proceeding so I can confirm.