# Create an all in one terminal based platform for arganising and updating the database.

# Can make use of a ui if it is more flexible then having a terminal based paltform.

# Options the system should have:
# Push to db verified images
# Sort images into per defect status in video folders
# Re-organise files after having separated them into per defect status.
# Merge relabeling csvs in to one based on inspection round
# Push relabled data to database
# Be able to print boundary boxes (pascal bb) on to image (IF su=ystem is UI based)
# Be able to delete duplicates from folders instead of using windows powershell
# Be able to check how many duplicates are in a given folder or folders

## Additionaly (Would be nice, if ui based)

# Implement the rest of the ondjila platform into the rest of the system.
# Implement IRI into the system as well if the system is ui based.
# Implement interpolation and and exporting of the geometry coords as well.

# Implement MCD automation as well.

# All in one desktoip application.